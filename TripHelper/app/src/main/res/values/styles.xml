<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CameraEditText" parent="Widget.AppCompat.EditText">
        <item name="android:colorControlActivated" tools:targetApi="lollipop">@color/fontCamera</item>
        <item name="android:textColor">@color/fontCamera</item>
        <item name="android:textCursorDrawable">@color/fontColor</item>
    </style>

</resources>
